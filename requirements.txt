Flask>=3.1,<4
opencv-python>=4.13,<5
numpy>=2.4,<3
Pillow>=12,<13
Werkzeug>=3.1,<4

# vmbpy is only required to use the Allied Vision driver (cameras/alliedvision_driver.py).
# It ships with the Allied Vision Vimba X SDK and is not available on PyPI,
# install it from the SDK's provided wheel, e.g.:
#   pip install vmbpy-<version>-py3-none-any.whl
